-- ============================================================================
-- 조교 등급 추가 — enum 값만 (2026-09-16 Alan 요청)
--
--  Postgres 는 새 enum 값을 **추가한 트랜잭션 안에서 쓸 수 없다**
--  (55P04 unsafe use of new value). 마이그레이션 하나가 한 트랜잭션이므로
--  값 추가는 이 파일에 혼자 두고, 실제로 쓰는 것은 다음 마이그레이션에서 한다.
-- ============================================================================

alter type public.user_role add value if not exists 'assistant';
