-- 랜딩 시간표의 750 레벨 메모 "월수금반 현장, 화목금반 인강" 을 비운다 (2026-09-17 Alan).
--
-- 인강 여부는 2026-09-17 부터 시간대마다 timetable_slots.ttf_recorded 로 적고(20260917090000),
-- 랜딩도 그 값을 시간대 줄 안에 표시한다. 레벨 전체에 붙던 이 메모는 그 값과 겹치는 데다
-- 카드 바닥에 있어 오전 반까지 인강처럼 읽혔고, 750 에만 있어 650 저녁이 빠져 있었다.
-- 컬럼은 남긴다 — 레벨 전체에 붙는 다른 안내에 쓸 수 있다. **인강 표시를 다시 여기에 적지 말 것.**
update public.timetable_levels
   set note = null
 where level = 750
   and note = '월수금반 현장, 화목금반 인강';
